services:
  mongodb:
    image: mongo:7
    container_name: medzu-mongodb
    ports:
      - '27017:27017'
    volumes:
      - medzu_mongo_data:/data/db
    restart: unless-stopped

volumes:
  medzu_mongo_data:
